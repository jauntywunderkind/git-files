# take oldest file & commit it
commitdd(){
	# take oldest file
	git ls-files -om | nohid | xargs ls -tr 2&>/dev/null | read old
	# subshell to preserve file naming
	( lastdd $old ; git add $old ; git commitd $* )
}
